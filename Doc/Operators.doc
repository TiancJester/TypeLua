-----------------------------
-- 算术运算符
-----------------------------
+      --加法
-      --减法
*      --乘法
/      --除法
%      --取模

-----------------------------
-- 字符拼接运算符
-----------------------------
..    --拼接左右字符串

-----------------------------
-- 比较运算符
-----------------------------

==    --等于,判断值类型相同或者复杂类型引用统一
~=    --不等于,判断值类型相同或者复杂类型引用不同
>     --大于,默认只适用于number类型
<     --大于,默认只适用于number类型
<=    --小于等于,默认只适用于number类型
>=    --大于等于,默认只适用于number类型

-----------------------------
-- 逻辑运算符
-----------------------------

and   --逻辑与
or    --逻辑或
not   --逻辑非

-----------------------------
-- 赋值运算符
-----------------------------
=    --使得一个值等于另一个值

this.a = this.b and c or d